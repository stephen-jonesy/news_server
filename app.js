const express = require("express");
const { getTreasures } = require("./controllers/app.controllers");

const app = express();

app.use(express.json());

app.get("/api/topics", );

module.exports = app;
